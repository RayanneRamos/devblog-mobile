import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    padding: 10,
    backgroundColor: '#232630',
    flexDirection: 'row',
    alignItems: 'center',
  },

  name: {
    color: '#fff',
    marginLeft: 8,
    fontSize: 18,
    fontWeight: 'bold',
  },
});